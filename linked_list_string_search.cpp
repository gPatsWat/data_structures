#include <bits/stdc++.h>
#include <cstddef>

using namespace std;

typedef struct Node {
    string str;
    Node* next;
} Node;

int Search(Node* head, string& str) {
    if (head == nullptr) return -1;

    Node* iter = head;
    int i = 0, j = 0;

    while (iter != nullptr && i < (iter->str).size()) {

    }
}

int main () {

}